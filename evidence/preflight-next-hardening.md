# Preflight Next Hardening

## Runtime Context

- Timestamp: `2026-06-25T14:16:09.4976380+02:00`
- OS: `Windows`
- Shell: `powershell`
- Working directory: `C:\Spec-kit_n8n`
- Known Proxmox host: `192.168.1.136`

## Repository State

- Present top-level project files:
  - `README.md`
  - `docs/proxmox-docker-runbook.md`
  - `examples/payload.example.json`
  - `scripts/build-workflow.mjs`
  - `scripts/container_pipeline.mjs`
  - `scripts/dry-run-local.mjs`
  - `scripts/remote_runner_orchestrator.sh`
  - `scripts/validate-local.mjs`
  - `workflows/spec-kit-opencode-proxmox-runner-orchestrator.json`
- Local evidence/probe artifacts created during this hardening run:
  - `.tmp/specify-probe-opencode/`
  - `.tmp/specify-probe-opencode-skills/`
  - `.tmp/uv-cache/`
- Git state:
  - `.git` path exists
  - `.git` entry count: `0`
  - `git status --short` failed with `fatal: not a git repository (or any of the parent directories): .git`
  - Classification: `STALE`

## Tooling Evidence

- Node: `v24.14.0`
- npm: `11.9.0`
- Python: `Python 3.14.3`
- uv: `uv 0.11.21 (5aa65dd7a 2026-06-11 x86_64-pc-windows-msvc)`
- OpenCode: `1.15.0`

Available shell/tool entrypoints:

- `powershell => C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe`
- `pwsh => MISSING`
- `bash => C:\Windows\system32\bash.exe`
- `wsl => C:\Windows\system32\wsl.exe`
- `sh => MISSING`
- `cmd => C:\Windows\system32\cmd.exe`
- `python => C:\Python314\python.exe`
- `node => C:\Program Files\nodejs\node.exe`
- `uv => C:\Users\xxammaxx\AppData\Local\hermes\bin\uv.exe`
- `opencode => C:\Users\xxammaxx\AppData\Roaming\npm\opencode.ps1`

Bash/WSL status:

- `bash --version` failed because no WSL distribution is installed
- `bash -n scripts/remote_runner_orchestrator.sh` therefore not runnable locally
- Classification: `TOOL_GAP`

## Workflow / Script Findings

- `node scripts/build-workflow.mjs` is available and returns a workflow file path
- `node scripts/validate-local.mjs` is available
- Workflow JSON appears importable:
  - valid JSON
  - required n8n nodes present
  - workflow regenerated byte-for-byte from `scripts/build-workflow.mjs --stdout`
- Secret scan result:
  - no hard-coded token/API-key/private-key patterns found in runtime files
  - Proxmox host `192.168.1.136` appears only as a host/config value
  - no credentials were found in workflow, scripts, README, runbook, or payload example
- Destructive command scan result:
  - banned runtime commands not found: `qm destroy`, `pct destroy`, `docker rm`, `docker volume rm`, `rm -rf /`, `git reset --hard`, `--yolo`
  - temp-dir cleanup remains limited to runner-local temp paths
- `spec_kit_plugin_module`:
  - still supported as optional override
  - no longer required for standard operation
- Official Spec Kit default installation:
  - now represented via `uv tool install specify-cli --from git+https://github.com/github/spec-kit.git@${SPEC_KIT_VERSION}`
  - verification commands represented: `specify version`, `specify integration list`
  - OpenCode init represented: `specify init --here --integration opencode --script sh --force`
- OpenCode slash-command fallback:
  - represented in code and docs via `.opencode/commands`

## Live Probe Evidence

Official CLI help and probe results collected locally:

- `uvx --from git+https://github.com/github/spec-kit.git specify --help`
  - exit `0`
  - commands confirmed: `init`, `check`, `version`, `integration`
- `uvx --from git+https://github.com/github/spec-kit.git specify init --help`
  - exit `0`
  - `--integration-options` confirmed
- `uvx --from git+https://github.com/github/spec-kit.git specify version`
  - exit `0`
  - local probe version output: `0.11.9.dev0`
- Probe init:
  - `specify init --here --integration opencode --script sh --force --ignore-agent-tools`
  - exit `0`
  - created `.opencode/commands/speckit.*.md`
- Probe skills attempt:
  - `specify init --here --integration opencode --integration-options="--skills" --script sh --force --ignore-agent-tools`
  - exit `1`
  - relevant output: `Unknown integration option '--skills'.`
- Probe integration list inside initialized project:
  - exit `0`
  - `opencode` shown as `installed (default)`

## Risk Classification

- `GREEN_SAFE`
  - local documentation updates
  - local validation updates
  - dry-run-only logic
  - evidence files
- `GREEN_SAFE` with future runtime impact but no live execution in this run
  - switch of default install semantics from plugin guesswork to official `uv` + `specify`
  - slash-command fallback verification logic
- `TOOL_GAP`
  - no local POSIX shell execution via WSL because no distro is installed
  - no live SSH/Proxmox validation in this run
- `STALE`
  - empty `.git` directory causing non-repository git failures

## Preflight Conclusion

Current local state after the reality refresh is suitable for `REAL_E2E_READY_PREPARED`:

- local artifacts are internally consistent
- official Spec Kit / OpenCode defaults are represented
- dry-run validation exists and is green
- live execution remains intentionally unverified without human approval and credentials
