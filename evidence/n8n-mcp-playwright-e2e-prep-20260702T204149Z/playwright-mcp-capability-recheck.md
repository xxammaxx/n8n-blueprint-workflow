# Playwright MCP Capability Recheck

## Environment
- **Node.js:** v22.22.0
- **Package:** @playwright/mcp@latest

## Key Options Available
- `--isolated` — keep browser profile in memory (available)
- `--headless` — run browser headless (available)
- `--browser` — browser or chrome channel selection (available)
- `--allowed-hosts` — restrict host access (available)
- `--allowed-origins` — origin allowlist (available)
- `--viewport-size` — custom viewport (available)

## Status
- **PLAYWRIGHT_MCP_CAPABLE**
- No tool gaps detected
- Isolated session mode confirmed
